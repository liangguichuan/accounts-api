servlet:
  port: 9999

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/accounts
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: WUseng2020
  redis:
    database: 0
    host: localhost
    port: 6379
    timeout: 100
    pool:
      max-active: 100
      max-wait: 100
      max-idle: 10
      min-idle: 0

mybatis:
  mapper-locations: classpath:mapper/*Mapper.xml
  type-aliases-package: com.chuan.accounts.dao.entity

logging:
  level:
    root: info
  path: D:\Environment\Log\accounts-log

